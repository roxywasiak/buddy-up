const { Student } = require("../../models");

const getStudent = () => {};

const updateStudent = (req, res) => {
  const id = req.sessions.user.id;
};

module.exports = {
  getStudent,
  updateStudent,
};
